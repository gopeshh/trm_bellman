# Stage B payloads

These eight files are the Stage B per-seed claim payloads copied from `payloads/seed-N.json`. They are labelled as payloads deliberately. Their canonical SHA-256 digests match `result/exp1b_result.json:payload_digests` in registered seed order.
