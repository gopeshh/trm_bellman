# Signed chain inputs

These are byte copies of the preserved run5 runtime authorization, execution admission, base-policy amendment, and chain declaration. `par_digests.txt` records the six preserved production PAR byte digests. The standalone checker recomputes document digests and follows their cross-document references; it does not treat a repeated digest string as proof by itself.
