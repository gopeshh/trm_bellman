# Stage A checkpoints

These eight checkpoint byte streams are copied from the sealed run5 evidence generation. The independent checker hashes each file and compares it with the corresponding recovered Stage A manifest. Their presence supports checkpoint identity and the downstream Stage B replay. It is not an independent training replication.
