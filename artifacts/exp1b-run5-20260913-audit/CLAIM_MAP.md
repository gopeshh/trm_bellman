# Experiment 1B run5 claim-to-artifact map

This map distinguishes artifact integrity, Stage B measurement reproduction, and claims the bundle does not establish.

| Claim | Support | Artifact | Check or limitation |
| --- | --- | --- | --- |
| The published result document has canonical SHA-256 `71f37c6f31d0bb9951b884f5f275e3b9c9bf07d46a836a1da37d9e80c6454e74`. | Supported | `result/exp1b_result.json`, `result/exp1b_result.sha256` | The independent checker canonicalizes the JSON and compares the computed digest with the sidecar. |
| All 35 links from registered study documents through authorization, admission, provenance, manifests, checkpoint bytes, payloads, and result close. | Supported | `chain_table.json`, `study_documents/`, `signed_chain/`, `result/`, `recovered_stage_a_manifests/`, `stage_a_checkpoints/`, `stage_b_payloads/` | The checker recomputes both ends of every link and separately verifies the recorded chain-table row. It does not trust the table's `observed` field. |
| The eight recovered files are the Stage A run manifests bound by provenance. | Supported | `recovered_stage_a_manifests/`, `result/provenance.json` | Canonical digests match `run_manifest_sha256s` in registered seed order. The directory README records their sealed source paths. |
| Each bundled Stage A checkpoint is the byte stream named by its recovered manifest. | Supported | `stage_a_checkpoints/`, `recovered_stage_a_manifests/` | The checker streams SHA-256 over every `checkpoint.pt` and compares it with `checkpoint_sha256` in the digest-bound manifest. |
| The eight files under `stage_b_payloads/` are Stage B payloads bound by the result. | Supported | `stage_b_payloads/`, `result/exp1b_result.json` | Canonical digests match `payload_digests` in registered seed order. They are not described as run manifests. |
| Run5 uses eight registered seeds in the published order. | Supported | `result/provenance.json`, recovered manifests, Stage B payloads | The checker compares inventory length, seed position, and seed ID across the three layers. |
| The eight published signed gaps follow `eps_n/(1-gamma) - (D + eps_m/(1-gamma))`. | Supported | `stage_b_payloads/seed-N.json`, `result/exp1b_result.json` | The checker recomputes each gap from the payload maxima and `gamma`, then requires bitwise equality with the payload, result vector, and result row. |
| The equal-weight mean signed gap is `14.248703798855473`. | Supported | `result/exp1b_result.json`, checker summary | The checker adds the eight binary64 gaps as exact rationals and performs one final binary64 division; the result is bitwise equal. |
| The frozen 95% bootstrap interval is `[12.185185177643433, 16.739775084598428]`. | Supported for the registered nominal bootstrap | `result/exp1b_result.json:interval`, checker summary | The checker decodes all 10,000 stored hex replicates and recomputes both Hyndman-Fan type-7 endpoints bitwise. This does not establish coverage beyond the registered procedure. |
| The effective trainer-parity bound is `5.76837158203125e-06`, and all eight reported maxima are below it. | Supported | Stage B payloads, result seed rows, checker summary | The checker evaluates `1e-6 + 4*2**-23*10` in binary64, compares payload and result parity values, and tests every value against the bound. |
| Stage B measurements reproduce from the eight existing checkpoints on the registered 128-state census. | Supported | `replay/output/seed-N.json`, `replay/recomputation.json`, replay logs | The replay exports all 128 states per seed. The independent consumer reports 108,648 exact comparisons with zero absolute, relative, and ULP difference for deterministic summary arithmetic. |
| Replay left the four model state dictionaries unchanged. | Supported | Per-state replay export headers and `replay/recomputation.json` | Each export records equal before/after state identities and `model_state_unchanged: true`; the independent consumer rejects a mismatch. |
| The same state maximizes both residual depths for all eight seeds. | Supported | `replay/recomputation.json` | State `exp1b-state-c45aa86d0f687807984b2ac8e2ed523f31c78572343c0c7d67d65da1d1200c16` is explicitly retained and verified for both depths in all seeds. |
| The preserved production PARs package the Stage B and parity-refusal source from revision `2a14eb6`, and the executed parity path raises. | Supported for packaged-source identity and source-path inspection | `logs/source-verification/embedded_source_inventory.json`, `logs/source-verification/report.md`, `signed_chain/par_digests.txt` | The inventory reports zero mismatches for commit-tracked project files in both PARs. The report identifies the four raising parity checks. This does not turn the replay PAR into a production PAR. |
| The current validation suites pass under Buck. | Supported with explicit skips | `logs/buck-test-exp1b.log`, `logs/buck-test-diagnostics.log` | Fresh runs report 336 Experiment 1B passes with 4 documented artifact-gated skips, and 46 diagnostics passes with no skips. |
| The checker fails closed when the published result changes. | Supported | `logs/checker-mutated-result.log`, `logs/mutation-byte-diff.log` | A temporary copy differed by one byte. The checker stopped at `result canonical digest` and exited 1. The mutated copy is not part of this bundle. |
| The eight Stage A training runs were independently replicated. | Not supported | None | The audit uses existing checkpoints and recovered manifests. It does not retrain models. |
| The replay proves the historical Stage B process executed exactly as recorded. | Not supported | None | Replay from existing checkpoints is measurement reproduction, not proof of historical execution. The digest chain establishes artifact relationships, not a historical event log. |
| The interval has coverage guarantees beyond the registered nominal two-sided percentile bootstrap. | Not supported | None | The bundle verifies the frozen computation and its inputs only. It does not establish broader statistical coverage. |
| Dataset preparation or properties beyond the identities recorded for this run were independently verified. | Not supported | None | The bundle checks bound digest fields. It does not rebuild, inspect, or validate the underlying datasets. |
| Base-policy preparation beyond the recorded run5 artifact identity and chain was independently verified. | Not supported | None | The bundle carries the amendment and identity references but does not reproduce base-policy training or selection. |
| The finite 128-state maxima are uniform theorem-domain bounds. | Not supported | None | They are finite-census maxima over the registered validation bridge, not uniform bounds. |
