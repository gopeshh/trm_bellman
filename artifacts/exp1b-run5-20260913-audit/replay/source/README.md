# Replay source

This directory preserves the two new replay entrypoints and the complete BUCK file whose digest is recorded in the build identities. These files identify the audit binaries. They are not represented as production source or production PARs.
