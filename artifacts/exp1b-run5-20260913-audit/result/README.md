# Published result and provenance

`exp1b_result.json` and `exp1b_result.sha256` are byte copies from the sealed run5 evidence generation. The sidecar is the SHA-256 of the result's canonical JSON encoding, not its raw file bytes. `provenance.json` binds the eight recovered Stage A manifests and checkpoint identities into this result chain.
